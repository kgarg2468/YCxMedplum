{
  "mcpServers": {
    "moss": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@moss-tools/mcp-server"
      ],
      "env": {
        "MOSS_PROJECT_ID": "${MOSS_PROJECT_ID}",
        "MOSS_PROJECT_KEY": "${MOSS_PROJECT_KEY}"
      }
    }
  }
}
